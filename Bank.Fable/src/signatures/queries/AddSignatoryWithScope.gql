mutation {
  addSignatory(
    input: {
      signatureOrderId: "[signatureOrder.id]"
      documents: [
        {id: "[signatureOrder.documents[0].id]"}
      ]
    }
  ) {
    signatory {
      href
    }
  }
}