mutation {
  deleteSignatory(
    input: {
      signatureOrderId: "[signatureOrder.id]"
      signatoryId: "[signatureOrder.openSignatory.id]"
    }
  ) {
    signatureOrder {
      openSignatory {
        status
      }
    }
  }
}