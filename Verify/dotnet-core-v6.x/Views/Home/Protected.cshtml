@{
    ViewData["Title"] = "ASP.NET Core v6 + Criipto Verify";
}
<h2>Welcome </h2>
 
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>