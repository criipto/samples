query querySignatureOrder($id: ID!) {
    signatureOrder(id: $id) {
        status

        signatories {
            id
            status
        }

        documents {
            blob
        }
    }
}